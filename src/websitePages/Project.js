import { Container, Typography } from "@mui/material";

const Project = () => {
    return (
        <Container>
            <Typography variant="h2">Projects</Typography>
            <Typography variant="body1">This is the Project Page</Typography>
        </Container>
    );
}
export default Project;