import { Container, Typography } from "@mui/material";

const Login = () => {
    return (
        <Container>
            <Typography variant="h2">Login</Typography>
            <Typography variant="body1">Please Login to access the Dashboard.</Typography>
        </Container>
    );
}
export default Login;